# 数据模型文档

## 核心表：tasks

只存放矩阵计算、筛选、排序真正需要用到的字段，保持精简：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid | 主键 |
| owner_id | uuid | 关联 auth.users |
| title | text | 任务标题 |
| description | text | 详细描述 |
| deadline_at | timestamptz \| null | 截止时间，UTC存储，可为空 |
| importance_level | int \| null | 重要性，1-5，可为空 |
| category_id | uuid \| null | 关联分类（P2引入） |
| recurrence_rule | text \| null | RRULE字符串 |
| recurrence_dtstart | timestamptz \| null | 循环起始时间 |
| completed_at | timestamptz \| null | 完成时间，null表示未完成 |
| created_at | timestamptz | |
| updated_at | timestamptz | 用于同步冲突解决（Last-Write-Wins） |

## 字段扩展原则

新的详情类字段（地点、人物等）按以下判断处理：

1. **一对一关系**：直接在 `tasks` 表加可空列。例如地点若只对应一个坐标，直接加 `location_place_id`、`location_name`、`location_lat`、`location_lng`。
2. **一对多关系**（例如一个任务关联多个相关人物）：必须用独立表，这是关系型数据库的硬性限制，不是设计偏好。
3. **需要对接外部标准API的字段**（地图、通讯录等）：即使当前一对一，只要该字段有自己的数据形状和索引需求，倾向于独立表，字段结构对齐外部API的数据结构（如 `place_id` 对应 Google Maps API，`contact_id` 对应系统通讯录API），减少未来对接时的转换成本。
4. **纯自定义、无固定结构、不对接任何外部系统的零碎信息**：使用 `custom_fields JSONB` 兜底字段。这是唯一合理使用JSONB的场景——已知结构的字段不应放入JSONB。

> 当前阶段（内测前）不必过度设计，凡是"能加列先加列"，真遇到一对多场景或表明显臃肿时再拆表。

## 扩展表示例（按需创建，不预先建立）

```
task_locations
  task_id (FK → tasks.id)
  place_id, name, lat, lng, address

task_people
  task_id (FK → tasks.id)
  contact_id, name, relation
```

详情页展示时，用统一的查询/拼装层（如 `TaskDetailAggregator`）分别查询这些扩展表并组装成详情对象；这是唯一"知道所有扩展表存在"的模块，`tasks` 核心表和其他扩展表互不感知彼此。

## 子任务（P2功能，暂不建表）

正式引入子任务功能时，通过一次明确的迁移操作，将 `tasks` 表"升级"为可容纳子任务的形态（如新增可空的 `parent_task_id` 自关联外键）。当前阶段不预先加此字段。

## 分类（P2功能）

```
categories
  id, owner_id, name, created_at
```

`tasks.category_id` 关联此表。矩阵视图默认查看所有分类，可按分类筛选。

## 团队协作（P3之后，未来功能，不预埋字段）

不在 `tasks` 表加任何团队相关字段。未来通过独立关联表实现：

```
task_collaborators
  task_id (FK → tasks.id)
  user_id (FK → auth.users.id)
  role
```

`tasks` 表本身不感知协作功能是否存在，符合开闭原则。

## 权限模型

Supabase RLS 基于 `owner_id` 判断可见性。当前仅支持个人任务；若未来开放协作，通过 `task_collaborators` 表补充RLS策略，不需要改动 `tasks` 表结构。
