# 技术架构文档

## 技术栈

| 层面 | 选型 | 理由 |
|---|---|---|
| Web前端 | Next.js（或 Vite + React） | Web优先级最高，需要最快的所见即所得反馈 |
| 移动端打包 | Capacitor | 复用Web代码直接打包安卓/iOS，无需重写UI，比React Native更贴合"网页优先"路线 |
| 后端/云同步 | Supabase（PostgreSQL + Auth + Realtime + RLS） | 与另一产品 Mindo 技术栈一致，工具链熟悉；RLS原生支持多用户数据隔离 |
| 循环任务 | rrule.js（RFC 5545标准） | 业界标准，未来对接日历系统天然兼容 |
| 开发环境 | 电脑为主 | 平板算力/工具链有限，vibe coding 全流程用电脑执行 |

## 架构原则

本项目遵循三条核心原则，目标是让未来新增功能、更换技术组件的成本尽量低：

### SRP（单一职责原则）

每个模块只负责一件事。例如：

- `UrgencyCalculator`：只负责紧迫度分档计算
- `RecurrenceEngine`：只负责循环任务下一次实例的计算
- `QuadrantClassifier`：只负责把（紧迫度，重要性）映射成象限标签
- `TaskRepository`：只负责任务数据的存取，不涉及业务规则判断

### ISP（接口隔离原则）

上层代码只依赖它需要的**窄接口**，不依赖具体实现细节：

- `ITaskRepository`：只暴露任务增删改查
- `IAuthProvider`：只暴露登录/登出/当前用户
- `ILocalStore` / `IRemoteStore` / `SyncEngine`：三者职责互相隔离，上层始终只调用统一的 `TaskRepository` 接口，感知不到背后是本地存储、远程Supabase还是同步队列

好处：未来更换后端服务、或Web端IndexedDB换成移动端SQLite，只需重新实现对应接口，不影响业务代码。

### OCP（开闭原则）：对扩展开放，对修改关闭

**不为"未来可能用到"的功能预先在核心表/核心逻辑里占位。** 新功能应该通过「新增独立表」或「新增可空字段」实现，禁止修改现有必填字段的语义或约束。核心实体（Task）不感知任何扩展模块（团队协作、子任务、日历视图）的存在，扩展模块单向依赖核心，核心不反向依赖扩展。

判断标准：一个新想法能不能只用"新增表 / 新增可空字段"实现、不碰现有表的必填约束？能，直接加；不能，才需要架构级讨论。

## 分层架构

```
┌─────────────────────────────────┐
│  表现层 (Presentation)            │  Next.js Web / 未来 Capacitor 壳
├─────────────────────────────────┤
│  应用层 (Application/Use-Cases)   │  业务流程编排，不关心数据存储细节
├─────────────────────────────────┤
│  领域层 (Domain/Core)             │  纯 TypeScript，零框架依赖
│  Task实体、紧迫度计算、重要性分档、  │
│  象限判定、循环规则引擎             │
├─────────────────────────────────┤
│  数据层 (Data/Repository)         │  接口 + 具体实现（Supabase/本地存储）
└─────────────────────────────────┘
```

## 数据层设计（Offline-First）

移动端（Capacitor打包后）需要离线可用：无网络时可查看/新建任务，联网后自动同步。

```
packages/data/
  ├── local/      本地存储实现（Web: IndexedDB；未来移动端: SQLite）
  ├── remote/     Supabase 通信
  ├── sync/       同步引擎：待同步队列 + 触发时机 + 冲突解决
  └── repository/ 组合以上三者，对外统一暴露 ITaskRepository
```

- `ILocalStore`：只管本地读写
- `IRemoteStore`：只管与 Supabase 通信
- `SyncEngine`：只管同步队列和触发时机（联网瞬间/定时/手动）
- 上层业务代码永远只调用 `TaskRepository`，不感知本地/远程/同步细节

**冲突解决策略（MVP阶段）**：Last-Write-Wins（以 `updated_at` 时间戳为准）。极端场景（两台设备同时离线编辑同一任务）可能丢失一方修改，个人任务管理场景概率低，可接受；后续如需要可升级为字段级合并。

## Monorepo 结构

延续 Mindo 项目的组织方式（pnpm + Turborepo）：

```
packages/core/     领域层，纯TS，无框架依赖
packages/data/     数据层实现（本地/远程/同步）
apps/web/          Next.js
apps/mobile/       未来 Capacitor 壳，直接复用 core + data
```

## 时间与日历兼容性

所有时间字段一律存储为 **UTC 时间戳**，不存储带日历语义的本地格式字符串（如"2026年8月17日"）。农历、伊斯兰历等展示需求属于表现层的可插拔转换模块（`ICalendarConverter` 接口，`GregorianConverter` / `LunarConverter` / `HijriConverter` 各自实现"UTC时间戳 ↔ 该历法显示格式"），与领域层完全解耦。日历视图（任务在日历上的位置）属于基于已有数据的查询结果，不是新增存储字段。
