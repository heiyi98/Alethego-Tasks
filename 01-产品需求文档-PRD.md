# 产品需求文档（PRD）

## 项目定位

一个内置「时间管理矩阵」的任务管理工具，采用 vibe coding 方式开发。

- **形态**：Web优先，未来扩展至安卓、iOS（同一套代码通过 Capacitor 打包）
- **数据**：云同步，支持多设备
- **开发环境**：电脑为主

## 上架优先级

Web → 安卓 → iOS（网页端可以最快看到效果，符合 vibe coding 快速验证的节奏）

## 功能路线图

### MVP（任务基础）

- 创建 / 编辑 / 删除任务
- 自定义定时循环任务
- 任务详情页：时间、关联人物、地点、详细描述等字段
- 时间管理矩阵：
  - 横轴（紧迫度）：由任务截止时间自动计算，用户不手动设置
  - 纵轴（重要性）：用户手动选择，5档
  - 矩阵可视化界面
  - 按象限筛选的列表视图（四个象限：重要且紧急 / 重要不紧急 / 紧急不重要 / 不重要不紧急）
- 分区（项目/分类）基础支持

> 提醒功能（定时提醒、提前提醒）已明确排除在当前范围外，暂不开发。

### P2（内测阶段）

- 子任务
- 分类（独立于MVP基础分区，做完整的分类管理）
- 搜索
- 数据备份/导出

### P3（内测阶段）

- 拖拽调整任务（象限间拖拽等）
- 视觉主题（含深色模式）

> P2、P3 为内测范围，正式上线（对外发布）在 P3 之后，因此当前阶段不需要担心数据迁移成本，可以按"现在需要什么就加什么"来推进。

## 时间管理矩阵产品定义

- 矩阵**不是**四个独立标签，而是一个由「紧迫度（X）× 重要性（Y）」构成的连续判定逻辑
- 象限（重要且紧急等）是根据 X、Y 计算结果**派生**出的展示分组，用于象限列表视图筛选，不是任务本身存储的字段
- 界面呈现上不需要连续坐标定位，用离散刻度 + 象限标签展示即可（不做散点图式的精确坐标摆放）
- 分类（项目/分区）与矩阵是正交维度：矩阵默认可查看所有分类的任务，也可筛选查看指定分类

## 未来规划（不在当前开发范围，但影响架构设计）

- 日历接入（含农历、伊斯兰历等）
- 团队任务协作
- 与另一产品 Mindo 的账号体系打通（仅账号互通，产品内容不互通）
- 未来可能有更多产品，参考"一个账号通行所有产品"模式（类似 QQ 账号体系）

详见《账号与多产品体系文档》与《数据模型文档》中对应的架构预留说明。
