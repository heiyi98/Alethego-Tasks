# 账号与多产品体系文档

## 登录方式

Supabase Auth 原生同时支持：

- 邮箱密码登录
- OAuth 第三方登录（Google / Apple）

MVP阶段两者都开启，属于配置层面的工作，无需额外开发。

## 与 Mindo 的账号互通需求

目标：**账号互通，产品内容不互通**。用户用同一账号登录 Mindo 和本任务管理应用（TaskApp），但两个产品的业务数据完全隔离。

### 当前阶段方案

两个产品共用**同一个 Supabase 项目**，用不同的 Postgres schema 隔离业务表：

```
同一个 Supabase 项目
  ├── auth.users              共享，两个产品用户体系统一
  ├── public schema           Mindo 现有业务表
  └── taskapp schema          TaskApp 业务表（新建）
```

- `auth.users` 天然共享 → 用户用同一账号登录两个产品，无需额外开发同步逻辑，真正的单点登录
- 业务数据分别隔离在各自 schema，互不干扰，符合开闭原则——TaskApp 的表结构变化不会影响 Mindo 现有表
- 唯一耦合点：两个产品共享同一 Supabase 项目的认证服务可用性和资源限额。现阶段用户量小，不构成问题

### 执行前置条件

检查 Mindo 现有的 `profiles` 表，确保只包含**通用信息**（昵称、头像、邮箱等），不混入 Mindo 专属的业务字段（如 BaZi 偏好设置）。若已混入，需要先做一次清理，将 Mindo 专属字段迁移到 Mindo 自己的业务表中。这一步是未来多产品架构能否顺利演进的关键前提。

## 未来演进路径（多产品，类似"一个账号通行所有产品"模式）

当产品数量增多、或某产品需要独立扩容/独立团队运营时，将账号体系正式拆分为独立的身份提供方（Identity Provider）：

```
【身份项目】独立 Supabase 项目
  只放：auth.users + profiles
  职责：登录、注册、签发 JWT token
        ↓ 签发token，各产品用共享密钥验证签名
【产品A: Mindo】独立项目/schema      【产品B: TaskApp】独立项目/schema     【产品C...】
  只放各自业务表                     只放各自业务表
  验证token签名，不直接连接身份项目数据库
```

关键原则：**产品之间、产品与身份系统之间不共享数据库连接，只共享一份 token 签名验证密钥**。任何一个产品的数据库需要重构或迁移，都不影响账号系统和其他产品。

## 现阶段建议

不需要现在就搭建独立的 Identity 项目，过度工程化没有必要。当前只需：

1. 清理 Mindo 的 `profiles` 表，确保通用信息与产品专属信息分离
2. TaskApp 上线时注册到 Mindo 所在的同一个 Supabase 项目，新建独立 schema
3. 待产品数量真正增多或出现独立运营需求时，再拆分为独立身份服务——由于 `profiles` 表从一开始保持干净，届时拆分成本很低
