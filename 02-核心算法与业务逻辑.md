# 核心算法与业务逻辑

## 1. 紧迫度（X轴）计算

### 理论依据

人类对时间/数值差异的感知遵循 **Weber-Fechner 定律**（对数感知定律）：感知差异与刺激的相对比例相关，而非绝对差值。这与「双曲线贴现」（Ainslie, 1975；Piers Steel 的时间动机理论）描述的现象在数学上是等价的——deadline 越近，紧迫感上升越剧烈；deadline 越远，紧迫感变化越平缓。

同样基于 Weber-Fechner 定律，敏捷开发的故事点估算采用斐波那契数列分档，相邻档位比例约为黄金比例 **φ ≈ 1.618**（约+60%）。本项目沿用同一原理，用斐波那契数列作为紧迫度分档依据。

### 紧迫度分档表（基本向量，14档）

刻度序号（tier_index）从 0 开始顺序排列；每档对应的"天数"取自斐波那契数列：

| 序号 | 对应天数 | 说明 |
|---|---|---|
| 0 | 0 | 今天 |
| 1 | 1 | 明天 |
| 2 | 2 | 后天 |
| 3 | 3 | 大后天 |
| 4 | 5 | 五天内 |
| 5 | 8 | 一周内 |
| 6 | 13 | 两周内 |
| 7 | 21 | 三周内 |
| 8 | 34 | 一个月内 |
| 9 | 55 | 两个月内 |
| 10 | 89 | 三个月内（一季度） |
| 11 | 144 | 半年内（两季度） |
| 12 | 233 | 三个季度内 |
| 13 | 377 | 一年内 |

**紧急/不紧急分界**：序号 0-6（0~13天）为「紧急」区，序号 7-13（21~377天）为「不紧急」区。两周（序号6）是紧急区的最后一格，即紧急/不紧急的分界点，按刻度序号对半计算得出（14档序号对半，非按天数数值求中位）。

### 扩展向量（超过一年，不进入基本判定逻辑）

用于展示极远期任务，但不参与四象限的紧急/不紧急判定：

| 对应天数 | 说明 |
|---|---|
| 610 | 两年内 |
| 987 | 三年内 |
| 1597 | 五年内 |
| 2584 | 七年内 |
| 4181 | 十年内 |

### 边界情况处理

- **无截止时间**：紧迫度固定为 0（不紧急端），任务仍可基于重要性进入矩阵展示
- **已逾期**（超过截止时间未完成）：紧迫度持续超出边界（不封顶），UI 单独标记为"逾期"样式
- **重要性和紧迫度都缺失**：不进入矩阵展示

### 关键实现原则

紧迫度**不是**创建任务时计算一次存死的值，而是**每次展示时基于当前时间动态计算**（因为距截止时间的天数会随时间推移变化）。

## 2. 重要性（Y轴）

- 用户手动选择，共 **5档**
- 具体档位名称/文案留待后续 UI 阶段确定，不影响当前架构

## 3. 象限判定逻辑

- 象限是根据「紧迫度所属区（紧急/不紧急）× 重要性所属区（重要/不重要，五档中的高位/低位划分方式待定）」计算得出的展示分组
- 不存储为任务字段，属于查询/展示层的派生逻辑

## 4. 循环任务

- 采用业界标准 **RFC 5545（iCalendar）RRULE** 描述循环规则，使用 `rrule.js` 库处理解析和下次发生时间计算
- 存储：`recurrence_rule`（RRULE字符串）+ `dtstart`（起始时间）
- 循环任务在不同视图下的展现方式不同：
  - **日历视图**（未来功能）：展示该循环规则下未来所有实例；若用户取消循环或修改循环规则，未来实例随之变化
  - **矩阵视图**：只展示同一循环中"最近的一个"任务实例
- 循环任务的重要性由用户设定后，各实例继承同一重要性
- 若某任务（含循环任务实例）既无重要性也无紧迫度，不进入矩阵展示
