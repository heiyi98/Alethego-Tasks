# 账号与多产品体系文档

## 登录方式

支持：

- 邮箱密码登录
- OAuth 第三方登录（Google / Apple）

## 与 Mindo 的账号互通需求（已确定方案：立即拆分独立身份项目）

目标：**账号互通，产品内容不互通**。用户用同一账号登录 Mindo 和本任务管理应用（TaskApp），但两个产品的业务数据完全隔离。

> 本方案已由"未来才做"调整为"现在就做"，原因：Mindo 目前处于内测阶段、无真实付费用户，现在迁移的风险和成本远低于上线后再做；且该架构本身是比"共享同一 Supabase 项目"更彻底的解耦方案，并非过度工程化。

### 架构：独立身份项目 + 分布式令牌验证

不采用"Mindo 签发、TaskApp 信任 Mindo"的两方模式——这仍会让 TaskApp 的登录能力依赖 Mindo 服务的可用性。采用**三方对等模式**：

```
【独立身份项目】—— 不属于 Mindo，也不属于 TaskApp，独立部署运行
  只放：auth.users + profiles
  职责：登录、注册、签发 JWT（私钥仅存在于此，不外流）
        ↓
【Mindo】                    【TaskApp】
  独立数据库                  独立数据库
  只验证令牌，不签发           只验证令牌，不签发
```

- 私钥集中在独立身份项目，签发是唯一的、集中的
- 验证是分布式的：Mindo 和 TaskApp 各自缓存身份项目公开发布的公钥（JWKS），本地验证令牌签名，不需要每次请求都联网询问身份项目——身份项目短暂不可用只影响"新登录"，不影响已持有有效令牌的用户继续使用
- 技术依据：Supabase 官方支持 **Third-Party Authentication** 功能——Supabase 项目可配置为信任外部签发的 JWT，不需要把用户迁移到本项目的 Auth 里；要求令牌使用非对称签名（RS256），并通过标准 OIDC 发现端点暴露公钥

### 国际 / 中国两侧完全独立（法律强制要求，非架构选择）

**中国境内外用户数据不能互通，这是法律约束，不是可调整的产品决策。** 因此实际落地是**两套完全独立的身份项目**，互不relate：

```
【国际身份项目】(Supabase.com)          【中国身份项目】(阿里云 RDS Supabase)
  ↓                                      ↓
Mindo 国际版 + TaskApp 国际版            Mindo 中国版 + TaskApp 中国版
```

- 国际侧：身份项目 + Mindo 国际版 + TaskApp 国际版，三方按上述"集中签发、分布式验证"运作
- 中国侧：身份项目部署在阿里云 RDS Supabase（阿里云官方托管的 Supabase 兼容服务，基于开源 Supabase 核心能力适配 RDS PostgreSQL）；**待确认**：RDS Supabase 托管版本是否开放"信任外部 JWT 签发方"这一具体配置项（认证模块目前确认支持接入 OAuth 第三方登录如 GitHub/支付宝，但那是"RDS Supabase 自己签发、借助第三方验证身份"，与本方案所需的"完全不签发、只信任外部身份项目"是不同的能力，需要直接向阿里云技术支持或详细产品文档核实）
- 两侧完全不共享私钥、不共享用户数据、不跨境通信，各自只负责自己辖区内的产品账号互通

### 执行前置条件

检查 Mindo 现有的 `profiles` 表，确保只包含**通用信息**（昵称、头像、邮箱等），不混入 Mindo 专属的业务字段（如 BaZi 偏好设置）。若已混入，需要先做一次清理。这一步是本次迁移能否顺利进行的关键前提，**需要在正式动工前完成**。

### 执行顺序（国际侧优先）

1. 清理 Mindo 国际版 `profiles` 表（前置条件）
2. 搭建国际身份项目（独立 Supabase 项目，仅含 `auth.users` + `profiles`），迁移 Mindo 国际版现有（内测阶段）用户数据
3. 确认 Mindo 国际版 Supabase 项目的 JWT 签名方式（需为 RS256 新式签名，若仍是 HS256 需先切换）
4. 将 Mindo 国际版项目从"自行签发"改造为"信任独立身份项目签发的令牌"（Third-Party Auth 配置）
5. TaskApp 国际版从第一天起使用全新独立 Supabase 项目，同样配置为信任独立身份项目
6. 中国侧待阿里云 RDS Supabase 技术细节确认后，按同样步骤在阿里云环境下复现

> 本次调整涉及 Mindo 现有认证架构的改造，需要与 Mindo 项目同步，不能仅在 TaskApp 这一侧推进。

## 历史记录

此前方案（两产品共用同一 Supabase 项目、不同 schema 隔离业务表）已被上述方案取代。原判断"不需要现在就搭建独立 Identity 项目，过度工程化没有必要"，因 Mindo 内测阶段无真实用户这一条件改变而调整。
